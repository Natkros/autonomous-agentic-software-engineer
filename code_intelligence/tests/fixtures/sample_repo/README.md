# Sample Repo

A tiny fixture application used only by the code_intelligence test suite.
It exposes a FastAPI backend with user authentication and a small React
frontend, so scanner/extractor/retrieval tests have something realistic
(if minimal) to index.
